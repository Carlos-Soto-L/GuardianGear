<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Tarjetas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #2c3e50;
            color: #ecf0f1;
        }

        .card {
            background-color: #8197ac;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 20px 0;
            transition: all 0.3s;
        }

        .card:hover {
            transform: scale(1.02);
        }

        .card-header, .card-body {
            border: none;
        }
    </style>
</head>

<body>

    <form action="/home" method="get">
        <button id="history-btn" class="btn btn-primary" type="submit">Regresar</button>
      </form>

    <div class="container mt-5">
        <div class="row">

            <% historial.forEach(function(hist) { %>
                <div class="col-lg-4 col-md-6 col-sm-12 mb-4">
                    <div class="card">
                        <h5 class="card-header">Usuario: <%= hist.sUsuario %></h5>
                        <div class="card-body">
                            <p><b>Inicio de viaje: </b><%= hist.sFechaRegistroInicio %></p>
                            <p><b>Último valor registrado de humedad: </b><%= hist.iValorSensorDH11Hum %> %</p>
                            <p><b>Último valor registrado de temperatura: </b><%= hist.iValorSensorDH11Tem %> C°</p>
                            <p><b>¿El conductor se abrochó el casco?: </b><% if (hist.iValorSensorIman == 1) { %>
                                SÍ <% } else { %> NO <% } %></p>
                            <p><b>¿Se registró un posible accidente?: </b><% if (hist.iValorSensorGolpe == 1) { %>
                                SÍ <% } else { %> NO <% } %></p>
                            <p><b>Último valor de longitud registrado: </b><%= hist.sLontitud %></p>
                            <p><b>Último valor de longitud registrado: </b><%= hist.sLatitud %></p>
                        </div>
                    </div>
                </div>
            <% }); %>

        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>